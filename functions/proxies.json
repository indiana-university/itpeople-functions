{
    "$schema": "http://json.schemastore.org/proxies",
    "proxies": {
        "apiUsers": {
            "matchCondition": {
                "route": "/api/users/{*id}"
            },
            "backendUri": "https://%API_HOST%/api/users/{id}"
        },
        "apiAuth": {
            "matchCondition": {
                "route": "/api/{*url}"
            },
            "backendUri": "https://%API_HOST%/api/{url}"
        },
        "spaManifest": {
            "matchCondition": {
                "route": "/manifest.json"
            },
            "backendUri": "https://%SPA_HOST%/app/manifest.json"
        },
        "spaAsset": {
            "matchCondition": {
                "route": "/static/{*url}"
            },
            "backendUri": "https://%SPA_HOST%/app/static/{url}"
        },
        "spa": {
            "matchCondition": {
                "route": "{*url}"
            },
            "backendUri": "https://%SPA_HOST%/app/index.html"
        }
    }
}
